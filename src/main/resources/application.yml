
server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/weione?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 111111
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
mybatis-plus:
    mapper-locations: classpath:mapper/*.xml
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
